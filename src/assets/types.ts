export type experienceType = {
    skills: string[];
    bullets: string[];
    description: string;
    title: string;
    period: string;
};

export type chartType = {
    id: number;
    value: number;
    label: string;
};